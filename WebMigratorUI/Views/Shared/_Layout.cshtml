<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <title>WebForms → DotVVM Migrator</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 2rem; }
    .card { border: 1px solid #ddd; padding: 1.5rem; border-radius: 8px; max-width: 720px; }
    label { display:block; margin:.5rem 0 .25rem; font-weight:600; }
    input[type=file], input[type=text] { width:100%; padding:.5rem; }
    button { margin-top:1rem; padding:.6rem 1rem; border-radius:6px; border:1px solid #333; background:#333; color:#fff; cursor:pointer; }
  </style>
</head>
<body>
  <div class="card">
    @RenderBody()
  </div>
</body>
</html>